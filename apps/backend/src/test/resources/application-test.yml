spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
    show-sql: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

  flyway:
    enabled: false

potriv:
  system-admin:
    email: test-admin@potriv.test
    password: TestAdminPassword1!
    name: Test System Admin
